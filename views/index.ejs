<!DOCTYPE html>
<html>
  <head>
    <title>Express App</title>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
      integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <style>
      table,
      thead {
        color: white;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div id="app">
        <h1 style="text-align: center;">
          Station: [<span id="x1"><%= station.x1 %></span>,<span id="y1"
            ><%= station.y1 %></span
          >], [<span id="x2"><%= station.x2 %></span>,<span id="y2"
            ><%= station.y2 %></span
          >], [<span id="x3"><%= station.x3 %></span>,<span id="y3"
            ><%= station.y3 %></span
          >]
        </h1>
        <canvas id="myChart"></canvas>
        <table class="table table-hover">
          <thead class="bg-info">
            <tr class="columns">
              <!-- Generated by Javascript -->
              <th>Entry ID</th>
              <th>Timestamp</th>
              <th>Phone ID</th>
              <th>Coordinates</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <!-- Generated by Javascript -->
            <% for (let i=0; i < entries.length; i++) { %>
            <tr>
              <td><%= entries[i].id %></td>
              <td><%= entries[i].time_stamp %></td>
              <td><%= entries[i].mac %></td>
              <td>[<%= entries[i].x %>,<%= entries[i].y %>]</td>
              <td><button class="btn btn-dark">Plot</button></td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
    <!-- Chart.js cdn -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@2.8.0"></script>
    <script src="/javascripts/script.js" defer></script>
  </body>
</html>
